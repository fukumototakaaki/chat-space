.main_chat
  .main-header
    .main-header__groupname
      = @group.name
    
    .main-header__member
      Member :
      - @group.users.each do |user|
        = user.name
      

  .edit
    = link_to edit_group_path(@group), class: "edit__btn" do
      Edit